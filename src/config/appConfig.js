const title = "App title"
import logoTop from '../resources/img/logo-top.png'
import logo from '../resources/img/logo.png'

const appConfig = {
  appTitle: `${title} ::  CMS`,
  defaultSize: 'xs',
  logoTop: logoTop,
  logo: logo
}

export default appConfig
